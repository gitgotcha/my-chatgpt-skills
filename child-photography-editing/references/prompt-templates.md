# 提示词模板

先序列化 `styleAuthority`，再序列化标记为“执行质量”的 `approvedTreatmentHints`。提示词必须声明：人物保护区域不可编辑；禁止全帧重生成；禁止换脸和改变五官、脸型、头型、眼型、眼距、鼻子、嘴型、耳朵、发际线、发型、发量、身体比例、四肢比例、手脚结构、年龄感、姿势、动作、服装和原始表情；哭泣必须保留；文字和元素不得遮挡人物。

当安全文字区不足时，删除或减少文字，不缩放人物腾出空间。Mask policy 只能是局部模式或 `person-protected-periphery`。
