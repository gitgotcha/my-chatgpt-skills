# QA 与安全回退

单图 QA 必须检查 `identity`、`childhood`、`expression`、`skin`、`artifact`、`theme`；批次 QA 还要检查 `batchConsistency`。人物变化、哭泣变表情、缺失头发、手脚/牙齿/肢体异常、服装误改、年龄感变化、塑料肤质、文字遮挡、背景穿帮或批次漂移均失败。

回退顺序：降低编辑强度 → 缩小蒙版 → 删除高风险元素 → 只改背景外围 → 改用传统调整 → 保留原图并报告原因。禁止用相同参数盲目重试，失败结果不得混入 `edited/`。
