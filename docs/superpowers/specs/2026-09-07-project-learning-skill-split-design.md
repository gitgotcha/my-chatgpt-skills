# 项目深度学习与开发方案学习 Skill 拆分设计

## 目标

将当前 PR #15 中混合在 `backend-project-learning` 内的两种教学需求拆开：

1. 保留 `backend-project-learning`，用于深度学习已实现的后端项目，并继续支持项目面试与 STAR 训练。
2. 新建 `development-plan-learning`，用于学习需求、架构规格、开发方案和实施计划；不包含面试或 STAR。

两个 Skill 都采用循序渐进的对话节奏，但学习目标与完成标准不同。

## Skill 边界

| Skill | 主要输入 | 默认目标 | 默认输出 | 不负责 |
|---|---|---|---|---|
| `backend-project-learning` | 后端仓库、真实源码、运行路径、数据库和消息链路 | 深入理解真实实现，并形成可用于项目面试的表达 | Mermaid、源码索引、分层讲解、成功与失败案例、面试问答、按需推进的 STAR | 学习尚未实施的开发方案本身 |
| `development-plan-learning` | 需求文档、架构规格、开发计划、ADR、流程说明；源码可作为现状证据 | 理解方案要解决的问题、组件职责、数据流、约束、取舍和实施顺序 | 图表或关系表、问题解释、贯穿案例、1—3 个核心理解题 | 面试训练、STAR、虚构实现、自动修改方案或源码 |

## 路由规则

- 用户要学习真实后端源码、调用链、数据库实现、异常恢复或项目面试时，使用 `backend-project-learning`。
- 用户要学习需求、开发方案、架构规格、实施计划、设计评审材料或未来版本时，使用 `development-plan-learning`。
- 同时提供规格与源码时，以用户当前问题为准：问“计划如何工作”走开发方案学习；问“代码实际如何做”走后端项目学习。
- 无法判断学习对象时只问一个问题，不同时加载两个完整教学流程。

## `backend-project-learning` 调整

以 `origin/main` 版本为能力基线，保留：

- 真实源码核验与准确文件行号；
- 完整业务闭环、成功与失败分支；
- 并发、一致性、事务、异步和性能深挖；
- 原理题、源码题、设计题和 STAR 训练；
- 用户明确授权后的源码学习注释。

教学节奏调整为分阶段推进：

1. 新模块先给小而闭合的 Mermaid 和学习索引。
2. 先讲业务职责与贯穿案例，确认用户能跟上。
3. 再进入关键源码、异常分支和专项机制。
4. 模块理解完成后进入面试题；用户作答后再给标准答案和追问。
5. STAR 放在模块末尾或用户要求时，不与首轮源码讲解同时倾倒。

用户追问局部问题时直接回答并返回主线；用户说停止时停止。保留深度内容，但分层揭示。

## `development-plan-learning` 设计

### 证据分层

每项结论标明来源：

- **现状事实**：由现有源码、配置、测试或部署证据验证；
- **方案要求**：规格或计划明确要求，但未必实现；
- **教学假设**：仅用于演示数据变化；
- **优化建议**：对方案的补充或替代，不能冒充已批准设计。

### 每个模块的教学闭环

1. 先用 Mermaid 或关系表建立当前模块的角色与边界。
2. 直接回答用户当前问题，解释输入、输出、状态和依赖。
3. 使用一个贯穿案例展示“之前—执行—之后”。
4. 涉及可靠性时增加一个最相关的失败、重试或并发分支。
5. 提出 1—3 个核心理解题，让用户用自己的话回答；先等待回答，不同时泄露答案。
6. 根据回答纠正具体误区，再进入下一模块。

同一模块的简短追问不重画全系统；关系变化时补局部图。图表、案例和正文各自承担不同信息，避免重复。

### 方案审阅边界

- 不能仅凭规格声称代码已实现或测试已通过。
- 当方案与源码冲突时指出差异，不擅自决定以哪一方为准。
- 解释锁、幂等、事务、队列或重试时说明成立条件，并用满足前提的例子说明。
- 默认只读；学习请求不授权修改方案、源码、部署或云端数据。
- 用户明确要求评审时可以列出缺口、风险和取舍；用户明确要求实施时转入相应开发工作流，不继续以教学模式代替实施计划。

## 文件与文档变更

- 恢复并节奏化调整 `backend-project-learning/SKILL.md`、README、复用提示词和 UI metadata。
- 新建 `development-plan-learning/`，包含 `SKILL.md`、README、`agents/openai.yaml`、复用提示词和行为测试场景。
- 更新仓库根 `AGENTS.md` 路由和 `README.md` 能力清单。
- 将当前 PR #15 的测试拆成两个 Skill 各自的包装完整性与行为场景；保留 UTF-8 和相对链接检查。
- 更新仓库级保护测试，使两个明确在本次变更范围内的 Skill 不被旧功能分支的保护列表误拦截，其他受保护模块保持不变。

## 行为测试

修改 Skill 前先运行对照样本，保存实际失败：

1. 深度项目学习场景：验证旧版本会不会一次倾倒所有层级；优化后应保留面试与 STAR，但循序推进。
2. 开发方案学习场景：在没有新 Skill 时，验证代理是否错误进入面试训练、把计划当实现或不给核心理解题。
3. 边界场景：规格与源码冲突、重复 Queue 通知、过期租约、用户停止、用户要求评审或实施。
4. 路由场景：同一主题分别询问“V2 计划如何工作”和“V1 源码如何实现”，应进入不同 Skill。

每个主要输出形状用多个新上下文样本人工审阅；自动测试只验证包装、编码、链接和仓库契约，不能替代行为测试。

## 验收标准

- `backend-project-learning` 仍具有原版本的深度源码、面试和 STAR 能力，但首轮不会一次倾倒全部环节。
- `development-plan-learning` 能围绕开发方案逐步解释问题，每个模块先有图表，并留下 1—3 个核心问题；不生成面试题或 STAR。
- 两个 Skill 的描述和路由互不抢占，规格与真实实现始终分开。
- 所有新增与修改资源为 UTF-8，相对链接有效，技能格式校验和仓库兼容性测试通过。
- 当前 PR #15 更新为上述拆分结果，不合并、不安装，等待用户审阅。
